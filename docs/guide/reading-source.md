<a id="reading-source"></a>
# 从源码中取得说明

作者可以在源文件注释中解释实现，也可以像本章一样单独编写叙述。
源码页保留代码的完整顺序，叙述页则由作者决定讲解哪些片段。

<a id="parse-source"></a>
## 将输入分成原文片段

解析入口先识别文档种类。Markdown 的文字直接成为正文；编程语言则由适配器取得可靠的注释边界。
接下来每个片段都保留来源，输出器可以复制代码，而不必重新打印语法树。

{% include "src/source.rs" id="parse" %}

名称路径从文件根出发，引用的是当前声明。增加不同名的声明不会改变这个选择；出现同名声明时，
作者需要审视歧义并显式选择当前源码顺序中的某一项。

[继续：在讲解中展开材料](expanding-directives.md.md#expanding-directives)
